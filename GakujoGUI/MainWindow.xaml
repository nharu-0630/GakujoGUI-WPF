<Window
    x:Class="GakujoGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    Title="GakujoGUI"
    Width="900"
    Height="600"
    ui:WindowHelper.UseModernWindowStyle="True"
    Loaded="Window_Loaded"
    mc:Ignorable="d">
    <Window.Resources>
        <ContextMenu x:Key="ContextMenu">
            <MenuItem Header="表示" />
            <MenuItem Header="更新" />
            <MenuItem Header="終了" />
        </ContextMenu>
    </Window.Resources>
    <Window.DataContext>
        <tb:TaskbarIcon
            x:Name="TaskBarIcon"
            ContextMenu="{StaticResource ContextMenu}"
            PopupActivation="All"
            ToolTipText="GakujoGUI"
            Visibility="Visible" />
    </Window.DataContext>
    <TabControl>
        <TabItem
            Name="LoginTabItem"
            Header="ログイン"
            IsSelected="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="180" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBox
                        Name="UserIdTextBox"
                        Width="Auto"
                        Margin="6"
                        ui:ControlHelper.PlaceholderText="静大ID" />
                    <PasswordBox
                        Name="PassWordTextBox"
                        Width="Auto"
                        Margin="6"
                        ui:ControlHelper.PlaceholderText="パスワード" />
                    <Button
                        Name="LoginButton"
                        Margin="6"
                        Click="LoginButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72A;" />
                    </Button>
                    <Label
                        Name="LoginDateTimeLabel"
                        Margin="6"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Column="1"
                    Width="5"
                    HorizontalAlignment="Stretch" />
                <DataGrid
                    Name="ClassTablesDataGrid"
                    Grid.Column="2"
                    Margin="6"
                    AreRowDetailsFrozen="True"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeColumns="False"
                    CanUserResizeRows="False"
                    CanUserSortColumns="False"
                    MouseDoubleClick="ClassTablesDataGrid_MouseDoubleClick"
                    RowHeight="96"
                    ScrollViewer.CanContentScroll="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Single"
                    SelectionUnit="Cell">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Monday}"
                            Header="月曜日"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Tuesday}"
                            Header="火曜日"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Wednesday}"
                            Header="水曜日"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Thursday}"
                            Header="木曜日"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Friday}"
                            Header="金曜日"
                            IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
        <TabItem Name="ClassContactsTabItem" Header="授業連絡">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <ui:AutoSuggestBox
                        Name="ClassContactsSearchAutoSuggestBox"
                        MinWidth="300"
                        Margin="6"
                        QueryIcon="Find"
                        QuerySubmitted="ClassContactsSearchAutoSuggestBox_QuerySubmitted"
                        SuggestionChosen="ClassContactsSearchAutoSuggestBox_SuggestionChosen"
                        TextChanged="ClassContactsSearchAutoSuggestBox_TextChanged" />
                    <Button
                        Name="ClassContactsLoadButton"
                        Margin="6"
                        Click="ClassContactsLoadButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                    </Button>
                    <Label
                        Name="ClassContactsDateTimeLabel"
                        Margin="6"
                        VerticalAlignment="Center"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Row="1"
                    Width="5"
                    VerticalAlignment="Stretch" />
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="100" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" MinWidth="100" />
                    </Grid.ColumnDefinitions>
                    <DataGrid
                        Name="ClassContactsDataGrid"
                        Grid.Column="0"
                        Margin="6"
                        AreRowDetailsFrozen="True"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserReorderColumns="False"
                        CanUserResizeRows="False"
                        CanUserSortColumns="False"
                        RowHeight="32"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        SelectionChanged="ClassContactsDataGrid_SelectionChanged"
                        SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="100"
                                MinWidth="100"
                                Binding="{Binding Subjects}"
                                Header="授業科目"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="280"
                                MinWidth="280"
                                Binding="{Binding Title}"
                                Header="タイトル"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="120"
                                MinWidth="120"
                                Binding="{Binding ContactDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                                Header="連絡日時"
                                IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <GridSplitter
                        Grid.Column="1"
                        Width="5"
                        HorizontalAlignment="Stretch" />
                    <ScrollViewer
                        Grid.Column="2"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DockPanel>
                            <StackPanel Orientation="Vertical">
                                <TextBox
                                    Name="ClassContactContentTextBox"
                                    Text="内容"
                                    Visibility="Collapsed" />
                                <TextBlock
                                    xmlns:link="clr-namespace:HyperlinkTextBlock"
                                    Margin="12"
                                    Text="{Binding ElementName=ClassContactContentTextBox, Path=Text}"
                                    TextWrapping="Wrap">
                                    <link:HyperlinkTextBlock.Inline>
                                        <Binding ElementName="ClassContactContentTextBox" Path="Text" />
                                    </link:HyperlinkTextBlock.Inline>
                                </TextBlock>
                                <StackPanel
                                    Name="ClassContactFilesStackPanel"
                                    Orientation="Horizontal"
                                    Visibility="Hidden">
                                    <ComboBox
                                        Name="ClassContactFilesComboBox"
                                        MaxWidth="360"
                                        Margin="6" />
                                    <Button
                                        Name="ClassContactOpenFileButton"
                                        Margin="6"
                                        Click="ClassContactOpenFileButton_Click">
                                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E5;" />
                                    </Button>
                                    <Button
                                        Name="ClassContactOpenFolderButton"
                                        Margin="6"
                                        Click="ClassContactOpenFolderButton_Click">
                                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE838;" />
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </DockPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Name="ReportsTabItem" Header="レポート">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button
                        Name="ReportsButton"
                        Margin="6"
                        Click="ReportsButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                    </Button>
                    <Label
                        Name="ReportsDateTimeLabel"
                        Margin="6"
                        VerticalAlignment="Center"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Row="1"
                    Width="5"
                    VerticalAlignment="Stretch" />
                <DataGrid
                    Name="ReportsDataGrid"
                    Grid.Row="2"
                    Margin="6"
                    AreRowDetailsFrozen="True"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeRows="False"
                    CanUserSortColumns="False"
                    RowHeight="32"
                    ScrollViewer.CanContentScroll="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionChanged="ReportsDataGrid_SelectionChanged"
                    SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding Subjects}"
                            Header="授業科目"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="280"
                            MinWidth="280"
                            Binding="{Binding Title}"
                            Header="タイトル"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding Status}"
                            Header="状態"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="120"
                            MinWidth="120"
                            Binding="{Binding StartDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                            Header="提出期間"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="120"
                            MinWidth="120"
                            Binding="{Binding EndDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                            Header="提出期間"
                            IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
        <TabItem Name="QuizzesTabItem" Header="小テスト">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button
                        Name="QuizzesButton"
                        Margin="6"
                        Click="QuizzesButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                    </Button>
                    <Label
                        Name="QuizzesDateTimeLabel"
                        Margin="6"
                        VerticalAlignment="Center"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Row="1"
                    Width="5"
                    VerticalAlignment="Stretch" />
                <DataGrid
                    Name="QuizzesDataGrid"
                    Grid.Row="2"
                    Margin="6"
                    AreRowDetailsFrozen="True"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeRows="False"
                    CanUserSortColumns="False"
                    RowHeight="32"
                    ScrollViewer.CanContentScroll="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionChanged="QuizzesDataGrid_SelectionChanged"
                    SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding Subjects}"
                            Header="授業科目"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="280"
                            MinWidth="280"
                            Binding="{Binding Title}"
                            Header="タイトル"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding Status}"
                            Header="状態"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="120"
                            MinWidth="120"
                            Binding="{Binding SubmissionStatus}"
                            Header="提出状況"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="120"
                            MinWidth="120"
                            Binding="{Binding StartDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                            Header="提出期間"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="120"
                            MinWidth="120"
                            Binding="{Binding EndDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                            Header="提出期間"
                            IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
        <TabItem Name="ClassSharedFilesTabItem" Header="授業共有ファイル">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <ui:AutoSuggestBox
                        Name="ClassSharedFilesSearchAutoSuggestBox"
                        MinWidth="300"
                        Margin="6"
                        QueryIcon="Find"
                        QuerySubmitted="ClassSharedFilesSearchAutoSuggestBox_QuerySubmitted"
                        SuggestionChosen="ClassSharedFilesSearchAutoSuggestBox_SuggestionChosen"
                        TextChanged="ClassSharedFilesSearchAutoSuggestBox_TextChanged" />
                    <Button
                        Name="ClassSharedFilesLoadButton"
                        Margin="6"
                        Click="ClassSharedFilesLoadButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                    </Button>
                    <Label
                        Name="ClassSharedFilesDateTimeLabel"
                        Margin="6"
                        VerticalAlignment="Center"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Row="1"
                    Width="5"
                    VerticalAlignment="Stretch" />
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="100" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" MinWidth="100" />
                    </Grid.ColumnDefinitions>
                    <DataGrid
                        Name="ClassSharedFilesDataGrid"
                        Grid.Column="0"
                        Margin="6"
                        AreRowDetailsFrozen="True"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserReorderColumns="False"
                        CanUserResizeRows="False"
                        CanUserSortColumns="False"
                        RowHeight="32"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        SelectionChanged="ClassSharedFilesDataGrid_SelectionChanged"
                        SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="100"
                                MinWidth="100"
                                Binding="{Binding Subjects}"
                                Header="授業科目"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="280"
                                MinWidth="280"
                                Binding="{Binding Title}"
                                Header="タイトル"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="120"
                                MinWidth="120"
                                Binding="{Binding UpdateDateTime, StringFormat=yyyy/MM/dd HH:mm}"
                                Header="更新日時"
                                IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <GridSplitter
                        Grid.Column="1"
                        Width="5"
                        HorizontalAlignment="Stretch" />
                    <ScrollViewer
                        Grid.Column="2"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DockPanel>
                            <StackPanel Orientation="Vertical">
                                <TextBox
                                    Name="ClassSharedFileDescriptionTextBox"
                                    Text="ファイル説明"
                                    Visibility="Collapsed" />
                                <TextBlock
                                    xmlns:link="clr-namespace:HyperlinkTextBlock"
                                    Margin="12"
                                    Text="{Binding ElementName=ClassSharedFileDescriptionTextBox, Path=Text}"
                                    TextWrapping="Wrap">
                                    <link:HyperlinkTextBlock.Inline>
                                        <Binding ElementName="ClassSharedFileDescriptionTextBox" Path="Text" />
                                    </link:HyperlinkTextBlock.Inline>
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <ComboBox Name="ClassSharedFileFilesComboBox" Margin="6" />
                                    <Button
                                        Name="ClassSharedOpenFileButton"
                                        Margin="6"
                                        Click="ClassSharedOpenFileButton_Click">
                                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E5;" />
                                    </Button>
                                    <Button
                                        Name="ClassSharedOpenFolderButton"
                                        Margin="6"
                                        Click="ClassSharedOpenFolderButton_Click">
                                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE838;" />
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </DockPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Name="ClassResultsTabItem" Header="成績情報">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button
                        Name="ClassResultsButton"
                        Margin="6"
                        Click="ClassResultsButton_Click">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                    </Button>
                    <Label
                        Name="ClassResultsDateTimeLabel"
                        Margin="6"
                        VerticalAlignment="Center"
                        Content="最終更新" />
                </StackPanel>
                <GridSplitter
                    Grid.Row="1"
                    Width="5"
                    VerticalAlignment="Stretch" />
                <DataGrid
                    Name="ClassResultsDataGrid"
                    Grid.Row="2"
                    Margin="6"
                    AreRowDetailsFrozen="True"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeRows="False"
                    CanUserSortColumns="False"
                    RowHeight="32"
                    ScrollViewer.CanContentScroll="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="160"
                            MinWidth="160"
                            Binding="{Binding Subjects}"
                            Header="教科名"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding TeacherName}"
                            Header="担当教員名"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding SubjectsSection}"
                            Header="科目区分"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding SelectionSection}"
                            Header="必修選択区分"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding SchoolCredit}"
                            Header="単位"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding Evaluation}"
                            Header="評価"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding Score}"
                            Header="得点"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="60"
                            MinWidth="60"
                            Binding="{Binding GP}"
                            Header="科目GP"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding AcquisitionYear}"
                            Header="取得年度"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding ReportDate, StringFormat=yyyy/MM/dd}"
                            Header="報告日"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Width="100"
                            MinWidth="100"
                            Binding="{Binding TestType}"
                            Header="試験種別"
                            IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
